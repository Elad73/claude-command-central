node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.local

# Editor / OS
.idea/
.vscode/
*.swp
*~

# Local runtime state (kept outside the repo but guard against accidents)
hook-debug.jsonl

# Playwright MCP scratch — baselines/after captures from local visual verification
.playwright-mcp/
baseline/
after/
